export const mapPropsToCharacter = (props) => {
  return {
    class: props.charClass,
    clothesColor: props.clothesColor,
    eyeColor: props.eyeColor,
    gender: props.gender,
    hairColor: props.hairColor,
    name: props.name,
    skinColor: props.skinColor,
  }
}
